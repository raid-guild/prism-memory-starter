[build]
builder = "RAILPACK"

[deploy]
startCommand = "python3 scripts/railway_entrypoint.py"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
